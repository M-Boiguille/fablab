apiVersion: v1
kind: ConfigMap
metadata:
  name: grafana-provisioning
  namespace: tools
data:
  datasource.yaml: |
    apiVersion: 1

    datasources:
      - name: Prometheus
        type: prometheus
        access: proxy
        url: http://prometheus.tools.svc.cluster.local:9090
        isDefault: true
