apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../apps/demo

patches:
  - path: patches/nginx-remove-command.yaml
    target:
      group: apps
      version: v1
      kind: Deployment
      name: nginx
