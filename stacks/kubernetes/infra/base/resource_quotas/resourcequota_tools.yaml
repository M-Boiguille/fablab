apiVersion: v1
kind: ResourceQuota
metadata:
  name: tools-ressources
  namespace: tools
spec:
  hard:
    requests.cpu: "400m"
    requests.memory: "256Mi"
    limits.cpu: "800m"
    limits.memory: "512Mi"
    pods: 4
